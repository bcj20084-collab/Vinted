import { prisma } from "@/lib/prisma"; import { ron,date } from "@/lib/format";
export const dynamic="force-dynamic";
export default async function Sales(){const items=await prisma.sale.findMany({include:{listing:{include:{product:true}}},orderBy:{soldAt:"desc"}});return <div className="grid gap-5"><div><p className="text-sm font-bold text-mint">REZULTATE</p><h1 className="text-3xl font-black">Vânzări</h1></div>{!items.length?<div className="card py-16 text-center"><h2 className="font-bold">Nicio vânzare încă</h2><p className="text-slate-500">Vânzările marcate vor apărea aici.</p></div>:<><div className="card desktop-table table-wrap"><table><thead><tr><th>Produs</th><th>Data</th><th>Preț</th><th>Profit net</th></tr></thead><tbody>{items.map(x=><tr key={x.id}><td><b>{x.listing.product.name}</b></td><td>{date(x.soldAt)}</td><td>{ron(Number(x.salePrice))}</td><td className={Number(x.netProfit)>=0?"text-emerald-700":"text-red-700"}><b>{ron(Number(x.netProfit))}</b></td></tr>)}</tbody></table></div><div className="mobile-cards grid gap-3">{items.map(x=><article className="card" key={x.id}><div className="flex justify-between gap-3"><b>{x.listing.product.name}</b><span className="text-sm text-slate-500">{date(x.soldAt)}</span></div><div className="mt-4 flex justify-between"><span>{ron(Number(x.salePrice))}</span><b className={Number(x.netProfit)>=0?"text-emerald-700":"text-red-700"}>{ron(Number(x.netProfit))} profit</b></div></article>)}</div></>}</div>}
