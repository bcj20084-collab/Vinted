import { prisma } from "@/lib/prisma"; import { ron,date } from "@/lib/format"; import { ListingForm } from "@/components/listing-form";
export const dynamic="force-dynamic";
export default async function Listings(){const [items,products]=await Promise.all([prisma.listing.findMany({include:{product:true},orderBy:{listedAt:"desc"}}),prisma.product.findMany({select:{id:true,name:true},orderBy:{name:"asc"}})]);return <div className="grid gap-5"><div><p className="text-sm font-bold text-mint">MAGAZIN</p><h1 className="text-3xl font-black">Listări</h1></div><ListingForm products={products}/>{!items.length?<div className="card py-16 text-center text-slate-500">Nu există listări.</div>:<div className="grid gap-3 md:grid-cols-2 xl:grid-cols-3">{items.map(x=><article className="card" key={x.id}><div className="flex justify-between gap-3"><div><h2 className="font-bold">{x.product.name}</h2><p className="text-sm text-slate-500">{date(x.listedAt)}</p></div><span className="h-fit rounded-full bg-cream px-3 py-1 text-xs font-bold">{x.status}</span></div><p className="mt-5 text-2xl font-black">{ron(Number(x.listPrice))}</p></article>)}</div>}</div>}
